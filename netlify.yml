build:
  command: hugo && env ; echo "$AWS_ACCESS_KEY" ; sleep 5
  environment: {}
context:
  deploy-preview:
    command: 'ls -alH ; free -h ; env ; hugo  || { sleep 120 ; false ; } '
plugins:
  - package: netlify-plugin-checklinks
redirects:
  - from: /foo
    status: 301
    to: /bar

